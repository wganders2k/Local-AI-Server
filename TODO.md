
### Chores
* Consolidate all model config/names/etc to one source of truth. Currently all this info is split across a few too many files (config.py, models.ini, system-prompts.ini, docker-compose.yml, etc.) This causes adding new models or tweaking configs to be a headache.

### Discord Bot
* Need to break ground on the discord bot side of things, currently I am paralyzed by the scope of the task. Start small with the channel message monitoring, get the bot hooked up to the proxy, worry about fine-tuning (history-service) later.
